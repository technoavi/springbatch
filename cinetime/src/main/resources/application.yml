server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost/cinetimedb?createDatabaseIfNotExist=true
    username: root
    password:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
